# MOODAY V4 · DESIGN_SPEC

> **MOODAY V4 产品设计规范（UI / UX）**

---

# 第一章 · 产品设计理念（Product Philosophy）

## 1.1 产品定位

MOODAY V4 是一套面向韩国市场的照片创作平台。

目标不是修图，而是让普通手机照片快速制作成具有纪念价值、艺术价值与礼物价值的实体作品。

产品设计遵循四个核心理念：

* 简单（Simple）
* 艺术（Artistic）
* 情感（Emotional）
* 成品（Product First）

所有页面、按钮、交互、模板，均以韩国用户的使用习惯与审美作为第一标准。

---

## 1.2 目标用户

主要用户：

* 韩国普通消费者
* 情侣、家庭、学生
* 礼物购买人群
* 手机拍照用户

用户特点：

* 不会 Photoshop
* 不懂修图
* 希望三分钟完成制作

因此，MOODAY 所有功能优先采用“一键完成”的设计思想。

---

## 1.3 六大产品

MOODAY 当前产品固定为六个：

1. 相纸相框
2. 艺术纸相框
3. 魔镜灯光画
4. 证件照
5. 免费头像
6. 免费壁纸

未来产品：

* 3D 光栅立体画
* 亚克力小夜灯
* 多层景深亚克力画
* 裸眼 3D 图像

未来新增产品不得重新开发编辑器，仅新增产品配置。

---

# 第二章 · 首页设计（Home）

## 2.1 首页定位

首页采用 **Banner + 六个产品入口** 的结构。

未来可升级为“产品体验中心”，但整体布局保持一致。

首页职责只有两个：

1. 展示产品
2. 引导进入制作

首页不承担编辑功能。

---

## 2.2 页面结构

首页采用四个固定区域，自上而下排列。

### ① Banner 轮播图

用于展示：

* 新产品
* 成品案例
* 节日活动
* 韩国风格作品

---

### ② 购买地址

位于 Banner 与产品入口之间。

显示内容：

**购买地址：MOODAY 쿠팡 스토어**

点击后进入用户的 Coupang 店铺（后续接入）。

本区域为永久保留区域。

---

### ③ 六个产品入口（2 × 3）

第一排：

* 相纸相框
* 艺术纸相框
* 魔镜灯光画

第二排：

* 证件照
* 免费头像
* 免费壁纸

所有入口采用统一卡片设计。

---

### ④ 页面底部

当前阶段不显示品牌信息。

底部区域预留，后续版本再扩展。

---

## 2.3 首页职责

首页只负责两件事情：

1. 展示产品
2. 引导进入对应产品

首页不承担编辑功能，不承担订单功能，不承担上传功能。


---

## 2.4 Banner 规范

Banner 用于展示：

* 新产品
* 节日活动
* 成品案例
* 韩国风格作品

Banner 不放复杂文字，突出视觉作品。

默认比例后续统一配置，不写死尺寸。

---

## 2.5 容器规范（Container）

> MOODAY V4 全局容器采用 **贴边设计**，所有页面统一执行。

### 设计原则

* 外层容器始终占满屏幕宽度（100%）
* 左右不留白边（贴边）
* 所有内容统一放入内容容器
* 内容容器左右内边距统一 **3%**
* 全部采用百分比计算，禁止固定像素

### 全局规范

| 项目         | 规范     |
| ---------- | ------ |
| 页面宽度       | 100%   |
| 左右外边距      | 0（贴边）  |
| 内容宽度       | 100%   |
| 左右 Padding | 3%     |
| 布局方式       | 百分比自适应 |

### 适用范围

以下区域全部使用统一容器：

* MOODAY 顶部栏
* Banner 轮播图
* Coupang 购买地址
* 六个产品入口
* 后续所有页面内容

### 永久规则

禁止在页面中写死：

* 16px
* 20px
* 24px

所有边距、宽度、位置统一由 `layoutConfig.js` 动态计算，实现手机、平板与不同尺寸设备的完全自适应。


# 第三章 · 上传页面设计（Upload Page）

> 上传页是所有产品统一入口，整个 V4 仅维护一套上传页面。

## 3.1 页面职责

上传页只负责四件事情：

1. 上传照片
2. 调整构图（拖动）
3. 双指缩放
4. 自动适配画布

不得出现任何编辑功能。

例如：

* Mood
* Style
* Decoration
* Template
* Clarity
* Optimize

全部禁止进入上传页。

---

## 3.2 页面结构

顶部：

* 返回按钮
* 当前产品名称

中部：

* 编辑画布（Canvas）

底部：

* 上传按钮
* 下一步按钮

整个页面保持极简。

---

## 3.3 画布原则

画布永远保持产品真实比例。

系统根据产品自动切换：

* 相纸相框
* 艺术纸相框
* 魔镜灯光画
* 证件照

用户无需手动设置比例。

---

## 3.4 手势规范

支持：

* 单指拖动
* 双指放大
* 双指缩小

禁止：

* 双击缩放
* 图片旋转
* 惯性拖动

整个操作保持稳定、简单。

---

## 3.5 证件照特殊规则

证件照上传页增加：

### 规格选择

支持：

* 30×40
* 35×45
* 50×70
* 51×51

规格选择位于上传按钮上方。

选择规格后，画布自动切换对应比例。

除此之外，与其它产品完全共用同一上传页面。

---

## 3.6 下一步原则

点击“下一步”后：

进入对应产品的编辑页面。

上传页不保存任何编辑效果，只负责图片初始化。

---

---

# 第四章 · 照片编辑页面（Editor Page）

> 编辑页是 MOODAY V4 的核心页面，所有创作功能统一在本页面完成。

## 4.1 页面职责

编辑页只负责照片创作。

包含功能：

* Mood（氛围）
* Style（风格）
* Decoration（装饰）
* Template（图文模板）
* Clarity（清晰）
* Optimize（优化）

不得出现：

* 相框
* 纸张
* 产品规格
* 提交订单

以上全部属于产品预览页。

---

## 4.2 页面结构

顶部：

* 返回
* 产品名称

中部：

* 编辑画布（Canvas）

底部：

六个功能按钮：

1. Mood
2. Style
3. Decoration
4. Template
5. Clarity
6. Optimize

点击按钮后，在底部展开对应内容，不跳转页面。

---

## 4.3 Mood（氛围）

作用：

改变画面的情绪与光影。

默认第一项：

**原图**

进入页面时默认选择原图，因此图片不会发生变化。

后续共 12 个氛围。

第一项永远为原图。

---

## 4.4 Style（风格）

作用：

改变画面的艺术风格。

默认第一项：

**原图**

进入页面默认保持原图。

后续共 12 个风格。

第一项永远为原图。

---

## 4.5 Mood 与 Style 叠加原则（永久）

Mood 与 Style 必须完全独立。

计算方式：

最终效果 = 原图 + Mood + Style

例如：

原图

↓

电影氛围

↓

胶片风格

最终得到：

电影氛围 + 胶片风格

如果 Style 改回原图：

仅取消 Style，

Mood 保持不变。

如果 Mood 改回原图：

仅取消 Mood，

Style 保持不变。

两者永远互不覆盖、互不影响。

---

## 4.6 Decoration（装饰）

作用：

在照片上添加装饰元素。

包含：

* 线条框
* 角框
* 角花
* PNG 装饰

Decoration 属于照片设计，不属于实体相框。

---

## 4.7 Template（图文模板）

作用：

艺术化文字排版。

包含：

* 标题
* 副标题
* 日期
* 艺术版式

Template 为 MOODAY 核心资产，不对外开放。

---

## 4.8 Clarity（清晰）

一句话：

**修画质。**

负责：

* 去噪
* 锐化
* 细节恢复
* 轻微模糊修复

采用一键清晰，不设置加强模式。

---

## 4.9 Optimize（优化）

一句话：

**修色彩。**

负责：

* 曝光
* 白平衡
* 对比
* 高光
* 阴影
* 色彩
* 肤色自然化

采用一键优化，不设置加强模式。

---

## 4.10 状态独立原则

编辑状态仅属于当前产品。

例如：

魔镜灯光画中的 Mood，

不得影响相纸相框中的 Mood。

每个产品进入编辑页时，都拥有自己的独立编辑状态。

---

---

# 第五章 · 产品预览页面（Preview Page）

> 产品预览页负责最终成品展示、产品配置与提交，不承担照片编辑功能。

## 5.1 页面职责

产品预览页只负责四件事情：

1. 产品配置
2. 实时成品预览
3. 场景展示
4. 提交订单（免费产品为下载）

本页面不得出现：

* Mood（氛围）
* Style（风格）
* Decoration（装饰）
* Template（图文模板）

所有编辑工作必须在上一页完成。

---

## 5.2 页面结构

顶部：

* 返回按钮
* 产品名称

中部：

* 实时成品预览（大图）

下方：

* 产品配置区

底部：

* 提交按钮（或下载按钮）

所有产品统一采用此布局。

---

## 5.3 相纸相框

产品配置包含：

### 相框

支持后续扩展：

* 白色
* 黑色
* 浅木
* 深木

### 相纸

支持：

* 高光
* 哑光
* 绒面
* 其它（预留）

切换任意选项时，预览立即更新。

无需点击应用。

---

## 5.4 艺术纸相框

页面结构与相纸相框完全一致。

区别仅在纸张：

支持艺术纸类型，例如：

* Fine Art
* Cotton
* Texture
* 其它（预留）

相框保持一致。

---

## 5.5 魔镜灯光画

魔镜不显示纸张与相框。

产品配置仅包含：

* 立式
* 底座

切换后立即更新成品预览。

预览必须接近真实产品展示效果。

---

## 5.6 实时场景预览

预览不是普通图片。

而是模拟真实产品。

例如：

相纸相框：

* 挂墙效果
* 桌面摆放
* 家居场景

魔镜灯光画：

* 点亮效果
* 夜间氛围
* 桌面展示

所有场景图统一存放于 Assets。

---

## 5.7 提交原则

实体产品：

按钮名称：

**Submit**

点击后进入订单提交流程。

免费产品：

按钮名称：

**Download**

不进入订单系统。

下载最终图片即可。

---

## 5.8 产品状态独立

不同产品拥有独立产品配置。

例如：

相纸相框选择了白色相框，

不得影响艺术纸相框。

每个产品进入预览页时，都初始化自己的产品配置状态。

---

# 第六章 · 证件照设计规范（Passport Photo）

> 证件照采用独立产品流程，但共用 MOODAY V4 编辑器架构。

---

## 6.1 产品定位

证件照的目标不是美颜，而是快速制作符合实际用途的标准电子证件照。

MOODAY V4 第一阶段固定四种规格：

| 韩文（默认） | 中文   |
| ------ | ---- |
| 여권사진   | 护照照片 |
| 증명사진   | 证件照  |
| 명함판    | 名片照  |
| 미국비자   | 美国签证 |

后续新增规格统一通过 `ProductConfig` 扩展，不修改页面结构。

---

## 6.2 页面流程

证件照共三个页面。

首页

↓

规格 + 上传

↓

照片编辑

↓

产品预览

↓

提交

整个流程与其它产品保持统一架构，仅功能不同。

---

## 6.3 第一页：规格 + 上传

本页面包含两个区域。

### ① 规格选择

固定四项：

* 여권사진
* 증명사진
* 명함판
* 미국비자

用户先选择规格，系统自动切换对应画布比例。

### ② 上传照片

支持：

* 上传照片
* 拖动位置
* 双指放大、缩小
* 自动居中构图

用户无需手动裁切，系统自动适配规格。

---

## 6.4 第二页：照片编辑

本页面仅包含两个功能。

### 去背景

* 一键去背景
* 实时显示效果

### 背景颜色

支持标准颜色：

* 白色
* 蓝色
* 灰色

切换颜色立即更新预览。

本页面不出现：

* Mood（氛围）
* Style（风格）
* Decoration（装饰）
* Template（图文模板）

保持证件照操作极简。

---

## 6.5 第三页：产品预览

本页面负责最终人物优化与提交。

包含：

### Clarity（清晰）

作用：

* 去噪
* 锐化
* 细节恢复

采用一键清晰。

### Optimize（优化）

作用：

* 曝光
* 白平衡
* 对比
* 高光
* 阴影
* 色彩
* 肤色自然化

采用一键优化。

优化目标是自然，不得过度磨皮。

本页面底部固定：

**Submit（提交）**

---

## 6.6 设计原则

证件照保持“三页完成”的设计理念。

* 第一步：选择规格 + 上传
* 第二步：去背景 + 背景颜色
* 第三步：清晰 + 优化 + 提交

所有交互遵循韩国用户习惯，以简单、一键、快速完成为最高原则。

---

# 第七章 · 设计系统（Design System）

> 本章定义 MOODAY V4 全局 UI 标准，所有页面统一遵守。

---

## 7.1 设计风格

MOODAY 面向韩国用户，整体视觉采用现代、简洁、温暖的韩国设计风格。

设计关键词：

* 简洁（Minimal）
* 高级（Premium）
* 留白（Clean）
* 圆角（Soft）
* 大图片（Photo First）

界面始终突出照片，而不是按钮。

---

## 7.2 颜色规范

### 主色（Primary）

用于：

* 主要按钮
* 当前选中
* 品牌强调

### 辅色（Secondary）

用于：

* 次级按钮
* 标签
* 辅助信息

### 中性色（Neutral）

用于：

* 背景
* 分割线
* 卡片
* 边框

具体颜色值统一写入 `themeConfig.js`，页面不得写死颜色。

---

## 7.3 字体规范

默认语言：韩文

字体优先级：

* 韩文
* 中文
* 英文

标题、正文、说明采用统一字号体系，由 `themeConfig.js` 管理。

页面禁止写死字号。

---

## 7.4 圆角规范

MOODAY 全局统一圆角。

统一应用于：

* 按钮
* 卡片
* 图片
* 弹窗
* 输入框

禁止同一页面出现多种圆角风格。

圆角尺寸由 Config 控制。

---

## 7.5 间距规范

统一采用 8px 网格系统。

常用间距：

* XS
* S
* M
* L
* XL

页面不得随意填写像素值。

所有间距统一读取 LayoutConfig。

---

## 7.6 按钮规范

按钮固定分三种。

### Primary

主操作：

* 下一步
* 提交
* 下载

### Secondary

次操作：

* 返回
* 取消
* 重置

### Option

选择按钮：

* 相框
* 纸张
* Mood
* Style
* 规格

所有按钮保持统一高度与圆角。

---

## 7.7 图标规范

图标风格统一采用：

* 线性（Outline）
* 简洁
* 圆角

禁止混用不同风格图标。

所有图标统一存放于：

`assets/icons`

---

## 7.8 响应式规范

MOODAY 必须支持：

* 手机竖屏（优先）
* 手机横屏
* 平板

设计原则：

画布始终最大化显示。

按钮区域自适应排列。

任何设备不得写死尺寸。

所有布局统一由 `layoutConfig.js` 动态计算。

---

## 7.9 一致性原则（永久）

任何新增页面必须保持统一：

* 颜色一致
* 字体一致
* 圆角一致
* 按钮一致
* 间距一致
* 动效一致

MOODAY 不允许同一产品出现两种设计语言。

---

# 第八章 · 产品规格规范（Product Specification）

> 本章定义 MOODAY V4 所有产品的规格标准，任何尺寸、纸张、产品类型均由 ProductConfig 统一管理。

---

## 8.1 设计原则

产品规格不属于页面。

产品规格统一由：

`config/productConfig.js`

管理。

页面只负责显示，禁止写死任何规格数据。

---

## 8.2 相纸相框

第一阶段规格：

| 名称    | 比例    |
| ----- | ----- |
| 4 × 6 | 2 : 3 |

第一阶段仅开放 4×6。

后续新增规格不得修改页面，仅新增配置。

---

### 相框颜色

支持：

* White
* Black
* Light Wood
* Dark Wood

默认进入：

White。

---

### 相纸类型

支持：

* Glossy（高光）
* Matte（哑光）
* Velvet（绒面）

默认：

Glossy。

---

## 8.3 艺术纸相框

第一阶段规格：

| 名称    | 比例    |
| ----- | ----- |
| 4 × 6 | 2 : 3 |

与相纸相框共用画布比例。

---

### 艺术纸类型

第一阶段预留：

* Fine Art
* Cotton
* Texture

具体纸张后续扩展。

---

## 8.4 魔镜灯光画

第一阶段规格：

| 名称           | 比例   |
| ------------ | ---- |
| 12 × 17.5 cm | 产品比例 |

产品形式：

* 立式
* 底座

默认：

立式。

魔镜不出现相框与纸张。

---

## 8.5 证件照

固定四种规格：

| 韩文   | 中文   |
| ---- | ---- |
| 여권사진 | 护照照片 |
| 증명사진 | 证件照  |
| 명함판  | 名片照  |
| 미국비자 | 美国签证 |

比例统一由 ProductConfig 管理。

---

## 8.6 免费头像

输出规格：

* 正方形头像

默认适配社交平台头像。

后续可增加更多尺寸。

---

## 8.7 免费壁纸

第一阶段：

* 手机竖屏壁纸

后续支持：

* 平板
* 桌面壁纸

所有比例统一配置。

---

## 8.8 扩展原则（永久）

未来新增产品，例如：

* 3D 光栅立体画
* 亚克力小夜灯
* 多层景深亚克力画
* 裸眼 3D 图像

不得修改页面结构。

仅允许新增：

* ProductConfig
* 产品预览素材
* 产品配置项

保持“一个编辑器，多产品”的永久架构。

---

# 第九章 · 编辑器布局规范（Editor Layout）

> 本章定义 MOODAY V4 编辑器的整体布局，所有产品统一采用同一套编辑器。

---

## 9.1 设计原则

编辑器采用 **上下双容器布局**。

整个页面只分两个区域：

1. 编辑画布容器（Canvas）
2. 底部功能容器（Tools）

两个容器职责完全独立，互不影响。

---

## 9.2 编辑器比例规范（Layout Ratio）

> MOODAY V4 以 **iPhone 12 / 13 / 14** 的屏幕比例作为设计基准，所有布局均采用百分比计算，禁止写死像素。

### 设计基准

| 项目     | 比例           |
| ------ | ------------ |
| 基准屏幕比例 | **19.5 : 9** |
| 页面宽度   | 100%         |
| 页面高度   | 100%         |
| 布局方式   | 百分比自适应       |

所有 Android、iPhone、平板均按照此比例动态缩放。

---

### 顶部导航栏（Header）

MOODAY 顶部品牌栏固定占页面高度：

**3.6%**

包含：

* MOODAY Logo
* 页面标题（预留）

高度采用百分比，不允许固定 30px。

---

### 内容容器（Content）

导航栏下方为统一内容容器。

规范：

| 项目         | 比例      |
| ---------- | ------- |
| 左右 Padding | **3%**  |
| 内容宽度       | **94%** |

所有页面统一采用贴边设计。

---

### Banner 比例

Banner 宽度 = 内容容器宽度（94%）

Banner 高度 = 宽度 × **0.45**

保持统一横幅比例。

---

### 画布比例（Canvas）

编辑画布固定采用：

> **高度 = 宽度 × 1.5**

即 **2 : 3** 产品比例。

系统根据不同产品自动适配，但画布始终保持真实比例，不允许拉伸。

---

### Banner 以下剩余区域

Banner 以下的剩余空间自动划分为 **3 等份**。

#### 第一份（1/3）

Coupang 店铺地址

显示：

**구매처 · MOODAY 쿠팡 스토어**

#### 第二、三份（2/3）

六个产品入口（2 × 3）

按钮区域自动占满剩余空间。

---

### 永久规则

整个首页高度计算固定为：

导航栏（3.6%）

↓

Banner

↓

剩余区域（3 等份）

* 1 份：Coupang 地址
* 2 份：六个产品按钮

任何设备均按百分比动态计算，禁止写死像素。


---

## 9.3 编辑画布（Canvas）

画布是整个页面的核心。

规则：

* 始终居中
* 最大化显示
* 保持产品真实比例
* 不允许拉伸变形
* 自动适配不同屏幕

不同产品仅改变比例，不改变布局。

---

## 9.4 底部功能容器

底部容器负责所有工具按钮。

特点：

* 固定吸底
* 圆角顶部
* 独立滚动（如需要）
* 不遮挡画布

编辑功能全部在此展开，不跳转页面。

---

## 9.5 工具展开方式

点击工具按钮后：

功能面板在底部容器内部展开。

例如：

* Mood
* Style
* Decoration
* Template
* Clarity
* Optimize

全部采用同一种展开方式。

禁止弹出不同风格窗口。

---

## 9.6 响应式原则

竖屏：

画布优先最大化。

横屏：

画布保持比例，工具区自动调整布局。

平板：

保持同一逻辑，不重新设计页面。

所有尺寸统一由 `layoutConfig.js` 动态计算。

---

## 9.7 永久规则

编辑器永远保持：

* 一个 Canvas
* 一个底部工具区
* 一个统一交互逻辑

不同产品只改变功能，不改变编辑器结构。

---

# 第十章 · 首页交互规范（Home Interaction）

> 本章定义 MOODAY V4 首页所有交互逻辑与统一规范。

---

## 10.1 页面层级

首页固定四个区域，自上而下排列：

1. MOODAY 顶部导航栏
2. Banner 轮播图
3. 구매처（官方购买入口）
4. 六个产品入口（2 × 3）

首页只承担产品展示与入口职责，不承担编辑、上传与订单功能。

---

## 10.2 Banner 轮播图

### 作用

用于展示：

* 新产品
* 韩国风格作品
* 成品案例
* 节日活动

### 交互规范

* 自动轮播
* 支持左右滑动
* 底部分页圆点
* 点击进入对应活动（预留）

Banner 图片统一由 `assets/images` 管理。

---

## 10.3 구매처（官方购买入口）

> 本规范为 **MOODAY V4 全局统一规范**。

### 显示内容

**구매처 · Coupang · MOODAY LAB**

显示规则：

* **구매처**：普通文字
* **Coupang**：品牌蓝色（唯一强调色）
* **MOODAY LAB**：官方店铺名称

### 交互规范

* 整个卡片可点击
* 点击进入 **MOODAY LAB** 官方 Coupang 店铺
* 店铺链接统一由 `appConfig.js` 管理
* 页面禁止写死链接地址

### 永久原则

凡是 MOODAY V4 出现官方购买入口，统一采用：

**구매처 · Coupang · MOODAY LAB**

不得出现其它命名或样式。

---

## 10.4 六个产品入口

采用统一 2 × 3 卡片布局。

### 第一排

* 相纸相框
* 艺术纸相框
* 魔镜灯光画

### 第二排

* 证件照
* 免费头像
* 免费壁纸

### 卡片规范

每张卡片统一包含：

* 产品图片
* 产品名称
* 圆角卡片
* 点击反馈动画

所有产品入口尺寸、圆角、间距保持一致。

---

## 10.5 点击逻辑

点击任意产品后：

统一进入 **Upload（上传页面）**。

不加载默认图片，不加载用户历史图片。

所有产品从空白编辑状态开始制作。

---

## 10.6 动画规范

首页仅允许轻量动画。

允许：

* Banner 滑动
* 卡片点击缩放
* 页面淡入

禁止：

* 复杂 3D 动画
* 长时间加载动画
* 影响操作速度的视觉效果

动画服务于体验，不干扰操作。

---

## 10.7 全局统一原则（永久）

首页所有交互必须保持统一：

* Banner 样式统一
* 구매처 样式统一
* 产品卡片统一
* 点击反馈统一
* 页面切换统一

MOODAY V4 全平台保持一致的韩国版设计语言，不允许同一产品出现两套交互逻辑。

---

# 第十一章 · 编辑器交互规范（Editor Interaction）

> 本章定义 MOODAY V4 编辑器所有交互行为，所有产品统一遵守。

---

## 11.1 编辑原则

编辑器遵循三个原则：

* 简单（Simple）
* 实时（Real-time）
* 非破坏（Non-destructive）

任何编辑操作都必须实时预览，并且可以随时恢复原图。

---

## 11.2 图片操作

支持三种基础手势：

### 单指

* 拖动图片位置

### 双指

* 放大
* 缩小

### 自动

* 图片自动居中
* 自动适配画布

禁止：

* 图片旋转
* 双击缩放
* 惯性滑动

---

## 11.3 工具切换

底部工具统一采用单选切换。

点击：

* Mood
* Style
* Decoration
* Template
* Clarity
* Optimize

仅展开当前工具，其它工具自动收起。

整个页面不跳转。

---

## 11.4 原图原则

每个工具第一项固定为：

**原图**

进入编辑页时默认状态：

* Mood = 原图
* Style = 原图

因此图片保持上传后的原始效果。

用户可以随时恢复原图。

---

## 11.5 实时预览

所有编辑必须即时生效。

例如：

* 更换 Mood
* 更换 Style
* 添加 Decoration
* 套用 Template
* Clarity
* Optimize

点击后立即更新 Canvas。

禁止增加“应用”按钮。

---

## 11.6 编辑状态保存

当前产品离开编辑页时，自动保存：

* 图片位置
* 缩放比例
* Mood
* Style
* Decoration
* Template
* Clarity
* Optimize

返回编辑页时恢复当前状态。

不同产品之间绝对隔离。

---

## 11.7 重置规则

每个工具拥有独立原图。

例如：

Style → 原图

仅取消 Style。

Mood 保持。

Mood → 原图

仅取消 Mood。

Style 保持。

任何工具不得覆盖其它工具状态。

---

## 11.8 永久原则

编辑器所有交互保持一致：

* 拖动方式一致
* 缩放方式一致
* 工具切换一致
* 实时预览一致
* 状态恢复一致

真正做到：

> **一个编辑器，多产品，共一套交互。**
